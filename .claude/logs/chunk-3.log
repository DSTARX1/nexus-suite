Chunk 3 done. Typecheck passes clean.

**What was built** in `competitor-worker.ts`:
- `reproducePost()` — fetches analysis from DB, chains script-agent → caption-writer → variation-orchestrator → `sendMediaJob()` → marks `reproduced = true`
- Replaced reproduce stub in worker switch with `reproducePost()` call
- Added imports for all 3 agents + `sendMediaJob`

**Pipeline flow:** analyze must run first (guards with error), then reproduce fetches stored analysis JSON, feeds it through 3 agents sequentially, parses FFmpeg transforms, queues `media:task`, marks post reproduced.
