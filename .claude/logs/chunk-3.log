Chunk 3/3 done. Summary of changes:

**New files:**
- `src/server/workflows/warming-workflow.yaml` — 4-phase warming definition (passive → engagement → posting → ready) using `warming.enqueuePhase` and `warming.markReady` actions with `dependsOn` chaining
- `src/cli/commands/warmup-start.ts` — CLI command that validates account (SECONDARY + proxy + fingerprint), enqueues pg-boss jobs across 3 phases with randomized `startAfter` schedules (8am-10pm spread), sets `warmupStatus=WARMING`

**Modified:**
- `src/cli/index.ts` — registered `warmup-start <accountId>` command
- `CLAUDE.md` — updated phase to `Chunk 3/3 done`

**tsc note:** `npm install` fails due to `next-auth@^5.0.0` not resolving — no tsc binary available. Manual type verification done against Prisma schema (`WarmupStatus`, `accountType`, `infisicalProxyPath`, `fingerprintProfile`) and existing warming module exports.
