Chunk 1 done. Created `src/server/modules/cli-bridge/cli-bridge.service.ts` with:

- **`execPocketCli(domain, service, action, args)`** — spawns Go binary via `execFile` (no shell injection), parses `--output json`, 60s timeout, 10MB buffer
- **`execPocketCliWithCredentials(...)`** — per-org credential bridging: fetches access/refresh tokens from Infisical, optionally fetches proxy URL, writes temp config to `os.tmpdir()` with `randomUUID` filename + `0o600` perms, executes CLI, finally block unlinks temp file + nulls all credential vars

`tsc` unavailable (deps can't install due to `next-auth@^5.0.0` version mismatch) — `|| true` expected.

CLAUDE.md updated: `build - Chunk 1/3 - #15`.
